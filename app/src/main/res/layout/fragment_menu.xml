<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".application.mvvm.view.MenuFragment">

    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/fragmentAdmin"
        android:layout_width="0dp"
        android:layout_height="0dp"
        tools:layout="@layout/fragment_pared"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@+id/bottomMenuBar"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <LinearLayout
        android:id="@+id/bottomMenuBar"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:background="?attr/colorPrimary"
        android:orientation="horizontal"
        app:layout_constraintTop_toBottomOf="@+id/fragmentAdmin"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent">

        <ImageButton
            android:id="@+id/btnPared"
            style="@style/MyImageButton"
            android:layout_width="70dp"
            android:layout_height="70dp"
            android:contentDescription="@string/button_pared"
            android:src="@drawable/pared_70" />

        <ImageButton
            android:id="@+id/btnPiso"
            style="@style/MyImageButton"
            android:layout_width="70dp"
            android:layout_height="70dp"
            android:contentDescription="@string/button_piso"
            android:src="@drawable/baldosa_70" />

        <ImageButton
            android:id="@+id/btnModo"
            style="@style/MyImageButton"
            android:layout_width="70dp"
            android:layout_height="70dp"
            android:contentDescription="@string/button_modo"
            android:src="@drawable/modo_70" />
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>